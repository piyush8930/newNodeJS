version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - echo "Installing dependencies..."
      - npm ci

  pre_build:
    commands:
      - echo "Running linter and security audits..."
      
  build:
    commands:
      - echo "Building the application..."
      - grep -Fq "Congratulations" index.html

  post_build:
    commands:
      - echo "Running tests..."
     
